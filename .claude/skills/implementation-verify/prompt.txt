Verify Android debug build compiles and show changes summary for user review.

Input: issue_number (optional)

Steps:
1. Build: ./gradlew assembleDebug
2. Check exit code: 0 = success, non-zero = compile failure
3. Prepare changes summary:
   - git diff --stat to get file changes
   - Count modified files
   - Summarize what was changed (ui, data, network, domain, etc.)
4. Show summary with:
   - Issue number and title
   - Files modified (with line counts)
   - Summary of changes
   - Test results (from previous step)
   - Build status
5. Pause and ask user:
   - Approve for commit?
   - Request more changes?
   - Abort?

Output format:
┌─────────────────────────────────────────┐
│ Issue #42: Add chore list screen        │
│ Verification Complete                   │
└─────────────────────────────────────────┘

Files Modified:
- app/src/main/.../ChoreListScreen.kt: +85, -0 lines
- app/src/main/.../ChoreListViewModel.kt: +62, -0 lines
- app/src/main/.../ChoreRepository.kt: +15, -2 lines
- app/src/main/.../Navigation.kt: +8, -1 lines

Summary:
Added ChoreListScreen Compose screen with ChoreListViewModel. Fetches chores from API via ChoreRepository and displays them in a LazyColumn. Wired into navigation graph.

Tests: ✅ All 45 tests passed
Build: ✅ assembleDebug succeeded

Ready to proceed?
- [Approve for commit]
- [Request changes]
- [Abort]
